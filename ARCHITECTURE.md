# 🏗️ System Architecture - Banking NL to SQL

## 📐 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        CLIENT INTERFACE                              │
├─────────────────────────────────────────────────────────────────────┤
│  • Web Browser (http://localhost:8000/docs)                         │
│  • cURL / PowerShell Scripts                                         │
│  • REST API Clients                                                  │
└────────────────────────────┬────────────────────────────────────────┘
                             │ HTTP POST /query
                             │ {"question": "..."}
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        FASTAPI SERVER                                │
│                     (main.py - Port 8000)                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  STEP 1: NL PARSER (Natural Language → SQL)                 │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │  • generate_sql_from_text()                                  │  │
│  │  • Injects banking schema context                            │  │
│  │  • Calls OpenAI GPT-4 (if available)                         │  │
│  │  • Fallback: Keyword-based generation                        │  │
│  │  • Returns: SQL + Explanation                                │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                             │                                        │
│                             ▼                                        │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  STEP 2: SQL VALIDATOR (Security Layer)                     │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │  • validate_sql()                                            │  │
│  │  • Uses sqlparse library                                     │  │
│  │  • Blocks: DELETE, INSERT, UPDATE, DROP, ALTER, etc.        │  │
│  │  • Allows: Only SELECT statements                            │  │
│  │  • Checks for SQL injection attempts                         │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                             │                                        │
│                             ▼                                        │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  STEP 3: LIMIT ENFORCER                                     │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │  • add_limit_if_missing()                                    │  │
│  │  • Auto-adds "LIMIT 1000" if not present                     │  │
│  │  • Prevents large result sets                                │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                             │                                        │
│                             ▼                                        │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  STEP 4: SQL EXECUTOR                                       │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │  • execute_sql()                                             │  │
│  │  • Connects to PostgreSQL (if configured)                    │  │
│  │  • Uses psycopg2 with RealDictCursor                         │  │
│  │  • Enforces 30-second timeout                                │  │
│  │  • Fallback: _execute_mock_query() with banking data        │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                             │                                        │
│                             ▼                                        │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  STEP 5: LOGGER & AUDITOR                                   │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │  • Logs all queries with timestamps                          │  │
│  │  • Records execution times                                   │  │
│  │  • Captures errors                                           │  │
│  │  • Audit trail for compliance                                │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                             │                                        │
└─────────────────────────────┼────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────────┐
        │         JSON RESPONSE                        │
        ├─────────────────────────────────────────────┤
        │  {                                          │
        │    "sql": "SELECT ...",                     │
        │    "explanation": "...",                    │
        │    "result": [...],                         │
        │    "execution_time_ms": 15.65               │
        │  }                                          │
        └─────────────────────────────────────────────┘
```

---

## 🗄️ Data Layer Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                    MOCK BANKING DATA                                 │
│                 (mock_banking_data.py)                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌───────────────────────┐  ┌──────────────────────┐               │
│  │   MOCK_CUSTOMERS      │  │  MOCK_TRANSACTIONS   │               │
│  ├───────────────────────┤  ├──────────────────────┤               │
│  │ • 100 records         │  │ • ~1,000 records     │               │
│  │ • Retail customers    │  │ • Deposits           │               │
│  │ • Premium clients     │  │ • Withdrawals        │               │
│  │ • Corporate accounts  │  │ • Wire Transfers     │               │
│  │ • Business accounts   │  │ • POS Purchases      │               │
│  │ • Student accounts    │  │ • Bill Payments      │               │
│  │                       │  │ • Multiple statuses  │               │
│  │ Fields:               │  │                      │               │
│  │ - customer_id         │  │ Fields:              │               │
│  │ - first/last name     │  │ - transaction_id     │               │
│  │ - email, phone        │  │ - customer_id (FK)   │               │
│  │ - city, state         │  │ - transaction_type   │               │
│  │ - account_type        │  │ - category           │               │
│  │ - customer_segment    │  │ - amount, currency   │               │
│  │ - credit_score        │  │ - date, time         │               │
│  │ - signup_date         │  │ - status, merchant   │               │
│  │ - account_balance     │  │ - location           │               │
│  │ - is_active           │  │                      │               │
│  └───────────────────────┘  └──────────────────────┘               │
│                                                                      │
│  ┌───────────────────────┐                                          │
│  │      MOCK_LOANS       │                                          │
│  ├───────────────────────┤                                          │
│  │ • ~120 records        │                                          │
│  │ • Personal Loans      │                                          │
│  │ • Home Mortgages      │                                          │
│  │ • Auto Loans          │                                          │
│  │ • Business Loans      │                                          │
│  │ • Student Loans       │                                          │
│  │ • Credit Cards        │                                          │
│  │                       │                                          │
│  │ Fields:               │                                          │
│  │ - loan_id             │                                          │
│  │ - customer_id (FK)    │                                          │
│  │ - loan_type           │                                          │
│  │ - principal_amount    │                                          │
│  │ - outstanding_balance │                                          │
│  │ - interest_rate       │                                          │
│  │ - term_months         │                                          │
│  │ - monthly_payment     │                                          │
│  │ - start_date          │                                          │
│  │ - status              │                                          │
│  │ - credit_score_at...  │                                          │
│  └───────────────────────┘                                          │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘

                            │
                            │ Loaded at startup
                            ▼
                ┌─────────────────────────┐
                │  _execute_mock_query()  │
                │  Smart data filtering:  │
                │  • Joins tables         │
                │  • Aggregates data      │
                │  • Filters by keywords  │
                │  • Sorts results        │
                │  • Applies LIMIT        │
                └─────────────────────────┘
```

---

## 🔄 Request Flow Diagram

```
┌──────────┐
│  Client  │
│  Browser │
└────┬─────┘
     │
     │ 1. POST /query
     │    {"question": "Show top customers by spending"}
     ▼
┌──────────────────────────────────────────────────────┐
│  FastAPI Endpoint: /query                            │
│  └─ Receives QueryRequest                            │
│  └─ Starts timer                                     │
└────┬─────────────────────────────────────────────────┘
     │
     │ 2. NL → SQL
     ▼
┌──────────────────────────────────────────────────────┐
│  generate_sql_from_text()                            │
│  ├─ Check OpenAI API key                             │
│  ├─ If available: Call GPT-4                         │
│  │  └─ Inject schema context                         │
│  │  └─ Get JSON response                             │
│  └─ Else: _generate_stubbed_sql()                    │
│     └─ Keyword matching                              │
│     └─ Return SQL + explanation                      │
└────┬─────────────────────────────────────────────────┘
     │
     │ 3. Validate
     ▼
┌──────────────────────────────────────────────────────┐
│  validate_sql()                                      │
│  ├─ Parse with sqlparse                              │
│  ├─ Check first token = SELECT                       │
│  ├─ Scan for dangerous keywords                      │
│  └─ Return (True, None) or (False, "Error msg")     │
└────┬─────────────────────────────────────────────────┘
     │
     │ 4. Add Safety
     ▼
┌──────────────────────────────────────────────────────┐
│  add_limit_if_missing()                              │
│  └─ If no LIMIT: append "LIMIT 1000"                 │
└────┬─────────────────────────────────────────────────┘
     │
     │ 5. Execute
     ▼
┌──────────────────────────────────────────────────────┐
│  execute_sql()                                       │
│  ├─ Check DB_CONNECTION_STRING                       │
│  ├─ If set:                                          │
│  │  ├─ Connect to PostgreSQL                         │
│  │  ├─ Execute with 30s timeout                      │
│  │  └─ Return results                                │
│  └─ Else: _execute_mock_query()                      │
│     ├─ Parse query keywords                          │
│     ├─ Filter MOCK_DATA                              │
│     ├─ Join tables if needed                         │
│     ├─ Aggregate if needed                           │
│     └─ Return results                                │
└────┬─────────────────────────────────────────────────┘
     │
     │ 6. Format Response
     ▼
┌──────────────────────────────────────────────────────┐
│  QueryResponse Model                                 │
│  ├─ sql: "SELECT ..."                                │
│  ├─ explanation: "..."                               │
│  ├─ result: [...]                                    │
│  └─ execution_time_ms: 15.65                         │
└────┬─────────────────────────────────────────────────┘
     │
     │ 7. Return JSON
     ▼
┌──────────┐
│  Client  │
│  Receives│
│  Results │
└──────────┘
```

---

## 🔐 Security Layer Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    SECURITY VALIDATION LAYERS                    │
└─────────────────────────────────────────────────────────────────┘

Layer 1: SQL Parsing
┌────────────────────────────────────────────────┐
│  sqlparse.parse(query)                         │
│  • Tokenizes SQL                               │
│  • Identifies statement type                   │
│  • Detects malformed queries                   │
└────────────────────────────────────────────────┘
                    │
                    ▼
Layer 2: Statement Type Check
┌────────────────────────────────────────────────┐
│  if first_token != 'SELECT':                   │
│      REJECT                                    │
│  Allowed: SELECT only                          │
│  Blocked: DELETE, INSERT, UPDATE, DROP,        │
│           ALTER, CREATE, TRUNCATE, etc.        │
└────────────────────────────────────────────────┘
                    │
                    ▼
Layer 3: Keyword Scanning
┌────────────────────────────────────────────────┐
│  Scan entire query for dangerous keywords:     │
│  • INSERT, UPDATE, DELETE                      │
│  • DROP, ALTER, CREATE                         │
│  • GRANT, REVOKE, EXEC, EXECUTE                │
│  • TRUNCATE, REPLACE                           │
│  If found: REJECT                              │
└────────────────────────────────────────────────┘
                    │
                    ▼
Layer 4: Limit Enforcement
┌────────────────────────────────────────────────┐
│  if 'LIMIT' not in query:                      │
│      query += ' LIMIT 1000'                    │
│  Prevents large result sets                    │
└────────────────────────────────────────────────┘
                    │
                    ▼
Layer 5: Execution Timeout
┌────────────────────────────────────────────────┐
│  statement_timeout = 30 seconds                │
│  Prevents long-running queries                 │
└────────────────────────────────────────────────┘
                    │
                    ▼
Layer 6: Read-Only Connection (Production)
┌────────────────────────────────────────────────┐
│  Use read-only database user                   │
│  • GRANT SELECT only                           │
│  • No INSERT/UPDATE/DELETE permissions         │
└────────────────────────────────────────────────┘
                    │
                    ▼
Layer 7: Audit Logging
┌────────────────────────────────────────────────┐
│  Log every query:                              │
│  • Timestamp                                   │
│  • Question asked                              │
│  • SQL generated                               │
│  • Execution time                              │
│  • Rows returned                               │
│  • Any errors                                  │
└────────────────────────────────────────────────┘
```

---

## 📊 Data Flow for "Top Customers by Spending"

```
User Question:
"Show me top 10 customers by total spending"
                │
                ▼
┌──────────────────────────────────────────┐
│  NL Parser (OpenAI or fallback)          │
│  Generates:                              │
│  SELECT c.first_name, c.last_name,       │
│         SUM(t.amount) as total_spent     │
│  FROM customers c                        │
│  JOIN transactions t                     │
│    ON c.customer_id = t.customer_id      │
│  WHERE t.status = 'completed'            │
│  GROUP BY c.customer_id,                 │
│           c.first_name, c.last_name      │
│  ORDER BY total_spent DESC               │
│  LIMIT 10                                │
└─────────────────┬────────────────────────┘
                  ▼
┌──────────────────────────────────────────┐
│  Validator: ✅ PASS                      │
│  • Statement type: SELECT                │
│  • No dangerous keywords                 │
│  • LIMIT present                         │
└─────────────────┬────────────────────────┘
                  ▼
┌──────────────────────────────────────────┐
│  Mock Executor:                          │
│  1. Aggregate MOCK_TRANSACTIONS          │
│     by customer_id                       │
│  2. Filter status = 'completed'          │
│  3. Join with MOCK_CUSTOMERS             │
│  4. Sort by total_spent DESC             │
│  5. Take top 10                          │
└─────────────────┬────────────────────────┘
                  ▼
Results:
[
  {"first_name": "Richard", "last_name": "Walker", 
   "total_spent": 48050.40},
  {"first_name": "Sarah", "last_name": "Smith", 
   "total_spent": 42601.09},
  ...
]
```

---

## 🏭 Deployment Architecture (Production)

```
                    ┌──────────────┐
                    │  Load        │
                    │  Balancer    │
                    └──────┬───────┘
                           │
           ┌───────────────┼───────────────┐
           │               │               │
           ▼               ▼               ▼
    ┌──────────┐   ┌──────────┐   ┌──────────┐
    │ FastAPI  │   │ FastAPI  │   │ FastAPI  │
    │ Instance │   │ Instance │   │ Instance │
    │    #1    │   │    #2    │   │    #3    │
    └────┬─────┘   └────┬─────┘   └────┬─────┘
         │              │              │
         └──────────────┼──────────────┘
                        │
                        ▼
                ┌──────────────┐
                │  PostgreSQL  │
                │  (Read-Only  │
                │   Replica)   │
                └──────────────┘
                        │
                        │ Replication
                        ▼
                ┌──────────────┐
                │  PostgreSQL  │
                │   Primary    │
                │  (Master DB) │
                └──────────────┘
```

---

## 🎯 File Structure

```
nl_to_sql_solution/
│
├── main.py                    # Core application
│   ├── QueryRequest           # Pydantic model
│   ├── QueryResponse          # Pydantic model
│   ├── generate_sql_from_text()
│   ├── _generate_stubbed_sql()
│   ├── validate_sql()
│   ├── add_limit_if_missing()
│   ├── execute_sql()
│   ├── _execute_mock_query()
│   └── FastAPI endpoints
│
├── mock_banking_data.py       # Data layer
│   ├── generate_mock_customers()
│   ├── generate_mock_transactions()
│   ├── generate_mock_loans()
│   ├── MOCK_CUSTOMERS [100]
│   ├── MOCK_TRANSACTIONS [~1000]
│   └── MOCK_LOANS [~120]
│
├── requirements.txt           # Dependencies
├── .env                       # Configuration
├── .env.example              # Config template
│
└── Documentation/
    ├── README.md             # Technical docs
    ├── QUICKSTART.md         # 30-sec guide
    ├── PRESENTATION_GUIDE.md # Demo walkthrough
    ├── DEMO_QUERIES.md       # Example questions
    ├── PROJECT_SUMMARY.md    # Overview
    └── ARCHITECTURE.md       # This file
```

---

**This architecture supports:**
- ✅ High performance (5-50ms queries)
- ✅ Enterprise security (multiple validation layers)
- ✅ Horizontal scaling (multiple instances)
- ✅ Database flexibility (PostgreSQL, MySQL, etc.)
- ✅ AI integration (OpenAI GPT-4)
- ✅ Offline capability (mock mode)
- ✅ Complete audit trails
- ✅ Production readiness
